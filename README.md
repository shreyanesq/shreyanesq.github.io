# Shreyan's Portfolio
Open `index.html` locally to preview the portfolio.

## GitHub Pages
Upload `index.html` to a GitHub repository, then go to Settings → Pages → Deploy from a branch → `main` → `/ (root)`. GitHub Pages will publish the static site.
